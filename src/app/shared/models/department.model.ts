export default class Department {
    name: string;
    department_code: string;

    constructor(name: string, department_code: string) {

        this.name = name;
        this.department_code = department_code;
    }
}
